# 🐾 Animora: Список задач (TODO)

---

## 🛠️ Этап 1: Инициализация и настройка проекта (Flutter + Supabase)

- [x] Инициализировать Flutter-проект в директории `c:\Projects` с названием `animora` и организацией `com.animora`.
- [x] Настроить `pubspec.yaml`:
  - Добавить зависимости: `supabase_flutter`, `go_router`, `flutter_riverpod`, `easy_localization`, `google_maps_flutter`, `font_awesome_flutter`, `uuid`.
- [x] Создать структуру папок согласно архитектуре:
  - `lib/core/` (темы, константы, утилиты)
  - `lib/data/` (модели, репозитории)
  - `lib/providers/` (стейт-менеджмент)
  - `lib/presentation/` (UI: общие экраны, владелец, провайдер)
- [x] Настроить мультиязычность (`easy_localization`):
  - Создать файлы локализации `assets/translations/ru.json` и `assets/translations/kk.json`.
  - Добавить конфигурацию в `pubspec.yaml`.

---

## 🗄️ Этап 2: Настройка базы данных (Supabase)

- [x] Создать SQL-скрипт (`supabase_schema.sql`) для развертывания структуры в консоли Supabase:
  - Таблицы: `profiles`, `pets`, `providers`, `services`, `bookings`.
  - Включить RLS (Row Level Security) политики для каждой таблицы.
  - Создать триггеры для автоматического создания профиля пользователя при регистрации.
- [x] Создать SQL-скрипт с демонстрационными данными (`supabase_seed.sql`) для клиник Астаны («Любимчик», «Ника», «VetSpectr», «Red Hot Chili Pets»).

---

## 🔐 Этап 3: Аутентификация и Навигация

- [x] Настроить инициализацию Supabase в `lib/main.dart`.
- [x] Реализовать `GoRouter` в `lib/app.dart` с поддержкой переключения ролей:
  - Маршруты для авторизации.
  - Маршруты для Владельца (`/owner/...`).
  - Маршруты для Провайдера (`/provider/...`).
- [x] Создать экран авторизации (`login_screen.dart`) по телефону (или email/пароль для быстрого тестирования в демо).
- [x] Создать экран выбора роли (`role_selection_screen.dart`) для новых пользователей.

---

## 📱 Этап 4: Разработка UI - Интерфейс владельца (Owner)

- [x] Создать главный экран владельца (`home_screen.dart`):
  - Карта (схематичная демо-карта Ишима и дорог Астаны) с маркерами клиник/салонов.
  - Горизонтальный список категорий (🏥 Ветклиника, ✂️ Груминг, 🐕 Выгул).
  - Карточки рекомендуемых заведений.
- [x] Создать страницу поиска/фильтрации (`search_screen.dart`).
- [x] Создать детальную карточку клиники (`provider_details_screen.dart`):
  - Список услуг с ценами.
  - Кнопка «Записаться».
- [x] Реализовать пошаговый процесс записи (`booking_flow` в виде BottomSheet в карточке клиники):
  - Выбор питомца -> Выбор услуги -> Выбор даты и времени -> Подтверждение.
- [x] Создать экран добавления и просмотра питомцев (`pet_list_screen.dart`).
- [x] Создать экран списка записей владельца (`booking_list_screen.dart`).

---

## 🏢 Этап 5: Разработка UI - Интерфейс клиники/салона (Provider)

- [x] Создать дашборд провайдера (`dashboard_screen.dart`):
  - Лента записей на сегодня.
  - Секция новых заявок с кнопками «Подтвердить» / «Отклонить».
  - Простая статистика (количество записей, доход за неделю).
- [x] Создать экран календаря на 14 дней с фильтрацией записей (`calendar_screen.dart`).
- [x] Создать экран управления услугами (`services_screen.dart`):
  - CRUD-операции для услуг (название, цена, длительность, переключатель активности).
- [x] Создать экран настроек профиля бизнеса (`provider_profile_screen.dart`).

---

## 🧪 Этап 6: Интеграция, Тестирование и Сборка демо-версии

- [x] Связать UI-компоненты с репозиториями Supabase через `Riverpod`.
- [x] Протестировать сквозной сценарий: Запись владельца -> Получение и подтверждение клиникой -> Отображение статуса у владельца.
- [ ] Развернуть бэкенд в Supabase и протестировать на реальном устройстве.
- [ ] Интегрировать реальные карты Google Maps/2GIS во Flutter после получения API ключей.
